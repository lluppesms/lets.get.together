# ------------------------------------------------------------------------------------------------------------------------
# Pipeline to build and deploy Azure Function and Resources using Bicep
# See readme file for info about the contents of the variable group
# ------------------------------------------------------------------------------------------------------------------------
name: $(date:yyyy).$(date:MM).$(date:dd)$(rev:.r) ${{ parameters.deployToEnvironment }}
pool:
  vmImage: ubuntu-latest
trigger:
  # since this repo has both AzDO and GHA pipelines, nothing is run automatically...
  - none
  # batch: true
  # branches:
  #   include:
  #     - main
  #   exclude:
  #     - '**/*.md'
  # paths:
  #   include:
  #     - 'src/function/**'

# ----------------------------------------------------------------------------------------------------
parameters:
  - name: deployToEnvironment
    displayName: Deploy To
    type: string
    values:
      - DEV
      - QA
      - PROD
      - DEV-QA-PROD
    default: DEV
  - name: deployBicep
    displayName: Deploy Bicep Infrastructure
    type: boolean
    default: false
  - name: runBuild
    displayName: Build Function App
    type: boolean
    default: true
  - name: runDeploy
    displayName: Deploy Function App
    type: boolean
    default: false
  - name: runUnitTests
    displayName: Run Unit Tests
    type: boolean
    default: true
  - name: runSmokeTest
    displayName: Run Smoke Test
    type: boolean
    default: false
  - name: runMSDevSecOpsScan
    displayName: Run MS DevSecOps Scan
    type: boolean
    default: true
  - name: runGHASScan
    displayName: Run GHAS Scan
    type: boolean
    default: false
  - name: addRoleAssignments
    displayName: Add Role Assignments
    type: boolean
    default: true

# ----------------------------------------------------------------------------------------------------
variables:
  - group: Dadabase.Demo
  - template: vars/var-service-connections.yml

# ----------------------------------------------------------------------------------------------------
stages:
- ${{ if ne(parameters.deployToEnvironment, 'DEV-QA-PROD') }}:
  - template: stages/bicep-and-function-stages.yml
    parameters:
      environments: ['${{ parameters.deployToEnvironment }}']
      deployBicep: ${{ parameters.deployBicep }}
      parseBicepOutputs: true
      runUnitTests: ${{ parameters.runUnitTests }}
      runSmokeTests: ${{ parameters.runSmokeTest }}
      runMSDevSecOpsScan: ${{ parameters.runMSDevSecOpsScan }}
      runGHASScan: ${{ parameters.runGHASScan }}
      deploymentType: 'functionapp'
      addRoleAssignments: ${{ parameters.addRoleAssignments }}

- ${{ if eq(parameters.deployToEnvironment, 'DEV-QA-PROD') }}:
  - template: stages/bicep-and-function-stages.yml
    parameters:
      environments: ['DEV','QA','PROD']
      deployBicep: ${{ parameters.deployBicep }}
      parseBicepOutputs: true
      runUnitTests: ${{ parameters.runUnitTests }}
      runSmokeTests: ${{ parameters.runSmokeTest }}
      runMSDevSecOpsScan: ${{ parameters.runMSDevSecOpsScan }}
      runGHASScan: ${{ parameters.runGHASScan }}
      deploymentType: 'functionapp'
      addRoleAssignments: ${{ parameters.addRoleAssignments }}
